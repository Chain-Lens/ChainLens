import app from "./server.js";

const PORT = Number(process.env.PORT ?? {{PORT}});
app.listen(PORT, () => {
  // eslint-disable-next-line no-console
  console.log(`[{{SELLER_NAME}}] listening on :${PORT}`);
});
